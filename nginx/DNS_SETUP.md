# 🌐 Настройка DNS для Pyland

## Информация о сервере

- **IP адрес**: 78.40.219.145
- **Домен**: pylandschool.com
- **Протокол**: HTTPS (обязательно)

## 📝 Необходимые DNS записи

Добавьте следующие записи в панели управления вашего доменного регистратора:

### A-записи

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | 78.40.219.145 | 3600 |
| A | www | 78.40.219.145 | 3600 |

### Альтернативный вариант (если поддерживается CNAME)

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | 78.40.219.145 | 3600 |
| CNAME | www | pylandschool.com. | 3600 |

> **Примечание**: В CNAME записи обязательна точка в конце домена!

## ✅ Проверка DNS записей

После добавления записей подождите 5-15 минут (время распространения DNS) и проверьте:

```bash
# Проверка основного домена
dig pylandschool.com +short
# Должен вернуть: 78.40.219.145

# Проверка поддомена www
dig www.pylandschool.com +short
# Должен вернуть: 78.40.219.145

# Альтернативная проверка (если dig не установлен)
nslookup pylandschool.com
nslookup www.pylandschool.com

# Или через ping
ping pylandschool.com
ping www.pylandschool.com
```

## 📋 Порядок действий

1. ✅ Добавьте DNS записи в панели регистратора
2. ⏳ Подождите 5-15 минут для распространения DNS
3. ✅ Проверьте DNS записи командами выше
4. ✅ Запустите скрипт установки на сервере
5. ✅ Получите SSL сертификат через certbot
6. ✅ Проверьте работу сайта: https://pylandschool.com

## 🔐 Получение SSL сертификата

После проверки DNS выполните на сервере:

```bash
# Убедитесь, что домен резолвится
dig pylandschool.com +short

# Получите SSL сертификат
sudo certbot --nginx -d pylandschool.com -d www.pylandschool.com

# Перезапустите Nginx
sudo systemctl restart nginx
```

## 🐛 Решение проблем

### DNS не резолвится

```bash
# Очистите DNS кеш локально
# MacOS:
sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder

# Linux:
sudo systemd-resolve --flush-caches

# Windows:
ipconfig /flushdns
```

### Certbot не может получить сертификат

**Ошибка**: "Challenge failed for domain pylandschool.com"

Причины:
1. DNS записи еще не распространились (подождите дольше)
2. Firewall блокирует порт 80 (проверьте `ufw status`)
3. Nginx неправильно настроен

Решение:
```bash
# Проверка портов
sudo ufw status
sudo ufw allow 'Nginx Full'

# Проверка конфигурации Nginx
sudo nginx -t

# Проверка, что домен доступен
curl -I http://pylandschool.com
```

### SSL сертификат получен, но сайт не открывается

```bash
# Проверьте статус сервисов
sudo systemctl status nginx
sudo systemctl status pyland-gunicorn

# Проверьте логи
sudo tail -f /var/log/nginx/pyland_error.log
```

## 📧 Дополнительные записи (опционально)

### SPF запись для email (если планируется отправка писем)

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| TXT | @ | v=spf1 mx ~all | 3600 |

### DMARC запись

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| TXT | _dmarc | v=DMARC1; p=none; rua=mailto:admin@pylandschool.com | 3600 |

## 🔍 Полезные ссылки

- [DNS Checker](https://dnschecker.org/) - проверка DNS по всему миру
- [SSL Labs](https://www.ssllabs.com/ssltest/) - проверка SSL конфигурации
- [Let's Encrypt Status](https://letsencrypt.status.io/) - статус Let's Encrypt

---

**Важно**: DNS изменения могут занять от 5 минут до 48 часов для полного распространения по всему миру, но обычно это происходит в течение 15 минут.
