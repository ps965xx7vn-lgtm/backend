# Управление подписками на уведомления

## Автоматическая подписка при регистрации

При регистрации на платформе **каждый новый пользователь автоматически подписывается на ВСЕ типы уведомлений**:

### ✅ Подписки активны по умолчанию:

1. **📧 Все email уведомления** - базовые уведомления платформы
2. **📚 Обновления курсов** - новые уроки и материалы
3. **⏰ Напоминания о уроках** - дедлайны и напоминания
4. **🏆 Уведомления о достижениях** - ваши успехи и прогресс
5. **📊 Еженедельная сводка** - статистика за неделю

### ❌ Подписка НЕ активна по умолчанию:

6. **🎁 Маркетинговые письма** - акции и специальные предложения

---

## Как управлять подписками?

### 1. Через настройки профиля

Перейдите в настройки вашего дашборда и управляйте подписками с помощью переключателей:

```
Дашборд → Настройки → Уведомления
```

Каждый тип подписки можно включить или выключить независимо.

### 2. Через ссылку отписки в письме

В каждом email-уведомлении есть ссылка для отписки внизу письма.

---

## Типы подписок соответствуют настройкам профиля

Типы подписок в системе **полностью соответствуют** настройкам в модели Student:

| Тип подписки | Поле в Student | По умолчанию |
|--------------|----------------|--------------|
| email_notifications | email_notifications | ✅ Включено |
| course_updates | course_updates | ✅ Включено |
| lesson_reminders | lesson_reminders | ✅ Включено |
| achievement_alerts | achievement_alerts | ✅ Включено |
| weekly_summary | weekly_summary | ✅ Включено |
| marketing_emails | marketing_emails | ❌ Выключено |

---

## Централизованная система

Все подписки хранятся централизованно в модели `notifications.Subscription`.

**НЕТ дубликатов в других приложениях** (blog, courses и т.д.).

Это обеспечивает:
- ✅ Единую точку управления подписками
- ✅ Согласованность с настройками пользователя
- ✅ Простую логику отписки/подписки
- ✅ Отслеживание источника подписки

---

## Для разработчиков

### Автоматическая подписка

Сигнал `subscribe_user_to_notifications()` в `authentication/signals.py` автоматически создает подписки при создании пользователя:

```python
@receiver(post_save, sender=User)
def create_user_student(sender, instance, created, **kwargs):
    if created:
        # ... создание профиля ...

        # Автоматическая подписка на все типы уведомлений
        subscribe_user_to_notifications(instance)
```

### Проверка подписки

```python
from notifications.models import Subscription

# Проверить подписку пользователя
has_subscription = Subscription.objects.filter(
    email=user.email,
    subscription_type='email_notifications',
    is_active=True
).exists()

# Или через метод модели User
has_subscription = user.has_active_newsletter_subscription()
```

### Создание подписки

```python
from notifications.models import Subscription

# Создать новую подписку
subscription, created = Subscription.subscribe(
    email='user@example.com',
    subscription_type='email_notifications',
    preferences={'source': 'manual'}
)
```

### Отписка

```python
# Отписать пользователя
Subscription.unsubscribe(
    email='user@example.com',
    subscription_type='email_notifications'
)
```

---

## Миграция

При переходе с старой системы (blog.Newsletter) на централизованную:

1. Данные мигрированы автоматически через `migrate_newsletter_data.py`
2. Все подписки из Newsletter преобразованы в Subscription с типом 'email_notifications'
3. Старая модель blog.Newsletter полностью удалена

---

## Вопросы и поддержка

Если у вас есть вопросы по управлению подписками, обратитесь в службу поддержки:
- Email: support@pyland.school
- Telegram: @pyland_support
