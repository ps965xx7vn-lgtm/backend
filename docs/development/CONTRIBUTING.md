# Руководство по разработке Pyland Backend

**Статус проекта:** Закрытый (Proprietary License)
**Владелец:** Дмитрий Масляев
**Последнее обновление:** 22 января 2026

---

## ⚠️ Важно

Этот проект является **собственностью Дмитрия Масляева** и защищен законом об авторском праве.

- ❌ **Запрещено** распространение кода без разрешения
- ❌ **Запрещено** использование в других проектах
- ✅ **Разрешено** для внутренней разработки команды
- ✅ Требуется подписание NDA для сторонних разработчиков

---

## 📚 Документация

Перед началом работы ознакомьтесь с:

- **[GIT_WORKFLOW.md](./GIT_WORKFLOW.md)** — полное руководство по Git Flow, commit guidelines
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** — архитектура проекта, структура кода
- **[README.md](../../README.md)** — общая информация и быстрый старт
- **[Быстрый старт](../getting-started/QUICK_START.md)** — локальная разработка

---

## 🚀 Начало работы

### 1. Получение доступа

Для работы с проектом необходимо:

1. **Получить приглашение** в репозиторий от владельца проекта
2. **Подписать NDA** (если вы сторонний разработчик)
3. **Настроить Git credentials**

```bash
# Клонирование репозитория (нужен access token)
git clone https://github.com/ps965xx7vn-lgtm/backend.git
cd backend
```

### 2. Установка окружения

```bash
# Установить зависимости через Poetry
poetry install

# Установить pre-commit hooks
poetry run pre-commit install

# Скопировать .env
cp .env.example .env

# Запустить БД и Redis (Docker)
docker-compose up -d db redis

# Применить миграции
poetry run python src/manage.py migrate

# Создать роли
poetry run python src/manage.py create_roles

# Создать superuser
poetry run python src/manage.py createsuperuser
```

### 3. Создать feature branch

``Переключиться на develop
git checkout develop
git pull origin develop

# Создать feature branch
git checkout -b feature/your-feature-name
```

---
## 💻 Процесс Разработки

---

## 💻 Процесс разработки

### 1. Стандарты кода

**Автоматическое форматирование** (через pre-commit):

- **ruff** — linting
- **black** — code formatting (line-length=100)
- **isort** — import sorting
- **mypy** — type checking

**Правила:**

```python
# ✅ Хорошо
def get_student_progress(student_id: int, course_id: int) -> dict[str, Any]:
    """
    Получает прогресс студента по курсу.

    Args:
        student_id: ID студента
        course_id: ID курса

    Returns:
        Словарь с данными прогресса

    Raises:
        Student.DoesNotExist: Если студент не найден
    """
    student = Student.objects.select_related('user').get(id=student_id)
    return calculate_progress(student, course_id)
```

### 2. Тестирование

**Обязательно** пишите тесты для новой функциональности!

```bash
# Запуск всех тестов
poetry run pytest

# С coverage report
poetry run pytest --cov=src --cov-report=html

# Конкретный модуль
poetry run pytest src/authentication/tests/

# Параллельно (быстрее)
poetry run pytest -n auto
```

### 3. Commit Messages

**Используйте Conventional Commits** (подробно в [GIT_WORKFLOW.md](./GIT_WORKFLOW.md)):

```bash
# Формат: <type>(<scope>): <subject>

# Примеры
feat(authentication): добавлена JWT аутентификация
fix(bloСоздание Pull Request

**Только для членов команды:**

```bash
# Push в origin (не fork!)
git push origin feature/your-feature-name

# Создать PR через GitHub UI или CLI
gh pr create \
  --base develop \
  --title "feat: краткое описание" \
  --body "Подробное описание изменений..."
```

**Шаблон PRat: краткое описание" \
  --body "Подробное описание изменений..."
```

**PR Template:**

```markdown
## Описание
Что делает этот PR?

## Изменения
- Добавлено X
- Исправлено Y

## Тестирование
- [x] Unit тесты пройдены
- [x] Проверено вручную

## Чеклист
- [x] Код соответствует style guide
- [x] Тесты добавлены
- [x] Документация обновлена

Closes #123
```

## 🔍 Code Review Process

### Для автора PR

1. Отвечайте на комментарии быстро и конструктивно
2. Вносите изменения по замечаниям
3. Resolve conversations после исправления

### Для reviewer

1. Проверьте код внимательно
2. Оставляйте кон

### Для автора PR

1. Отвечайте на комментарии оперативно
2. Вносите исправления по замечаниям
3. Отмечайте resolved conversations

### Для reviewer

1. Проверяйте код тщательно
2. Оставляйте конструктивную обратную связь
3. Approve только когда уверены в качестве

---

## 📋 Чеклист перед слиянием
```markdown
- [ ] Получено одобрение от лид-разработчика
- [ ] Документация обновлена

---

## 🐛 Отчеты об ошибках

Создайте GitHub Issue с описанием:

```markdown
**Описание проблемы:**
Что произошло?

**Ожидаемое поведение:**
Что должно было произойти?

**Шаги для воспроизведения:**
1. Сделать X
2. Нажать Y
3. Увидеть ошибку

**Окружение:**
- OS: macOS 14
- Python: 3.13
- Django: 5.2
```

---

## 💡 Предложения по улучшению

Создайте GitHub Issue с описанием:

```markdown
**Описание фичи:**
Что хотите добавить?

**Зачем это нужно:**
Какую проблему решает?

**Возможная реализация:**
Как можно это сделать?
```
## 📝 Документация

### API Документация

Django Ninja генерирует автоматическую документацию:

```bash
---

## 📝 Документирование

### API Документация

Django Ninja генерирует автоматическую OpenAPI документацию:

```bash
# Запустить сервер
poetry run python src/manage.py runserver

# Открыть в браузере
# http://127.0.0.1:8000/api/docs
```

### Код и комментарии

- Все docstrings на русском
- Используйте Google Style для docstrings
- Комментируйте сложную логику
- Обновляйте README при изменении архитектуры

---

## 🔐 Безопасность и конфиденциальность

### Обязательства команды

- ✅ **НЕ делиться** кодом за пределами команды
- ✅ **НЕ публиковать** фрагменты кода в соцсетях/блогах
- ✅ **НЕ использовать** код в личных проектах
- ✅ **Хранить** .env и credentials локально (не commit!)
- ✅ **Соблюдать** условия NDA после ухода

### Для сторонних разработчиков

1. **Подписание NDA** обязательно перед доступом к коду
2. **Ограниченный срок** доступа (по контракту)
3. **Удаление** локальных копий после завершения работы

---

## 👥 Команда разработки

**Владелец проекта:** Дмитрий Масляев
**Email:** limpoposiki@gmail.com

### Роли в команде

- **Owner** — полный доступ ко всему
- **Lead Developer** — review и approve PR
- **Developer** — создание feature branches и PR
- **Contractor** — временный доступ по NDA

---

## 🤝 Правила работы в команде

- Будьте уважительны к коллегам
- Конструктивная критика приветствуется
- Помогайте друг другу
- Соблюдайте конфиденциальность проекта
- Нет токсичности

---

## 📚 Полезные ресурсы

- [GIT_WORKFLOW.md](./GIT_WORKFLOW.md) — полный Git workflow guide
- [ARCHITECTURE.md](./ARCHITECTURE.md) — архитектура проекта
- [Django Ninja Docs](https://django-ninja.rest-framework.com/)
- [pytest-django Docs](https://pytest-django.readthedocs.io/)

---

**© 2025-2026 Дмитрий Масляев. Все права защищены.**

*Этот документ является конфиденциальным и предназначен только для членов команды разработки Pyland Backend.*
