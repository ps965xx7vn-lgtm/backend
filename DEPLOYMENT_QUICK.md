# ⚡ Быстрый Деплой

## Процесс деплоя в 4 шага:

### 1️⃣ Подготовка локально (ОБЯЗАТЕЛЬНО!)

```bash
# Запустите скрипт подготовки
./nginx/scripts/prepare_deploy.sh
```

Скрипт автоматически:
- ✅ Проверит конфигурацию nginx
- ✅ Проверит git статус
- ✅ Предложит выбрать ветку (main/dev)
- ✅ Создаст коммит (если есть изменения)
- ✅ Запушит на GitHub

### 2️⃣ Настройка DNS

Добавьте A-записи:
- `pylandschool.com` → `78.40.219.145`
- `www.pylandschool.com` → `78.40.219.145`

### 3️⃣ Установка на сервер

```bash
ssh root@78.40.219.145

# Клонируйте из нужной ветки
mkdir -p /opt/pyland && cd /opt/pyland
git clone -b main https://github.com/ps965xx7vn-lgtm/backend.git
# или: git clone -b dev для тестирования

# Запустите установку
cd backend/nginx/scripts
sudo ./install.sh
```

### 4️⃣ Финальная настройка

```bash
# Настройте .env
nano /opt/pyland/backend/.env

# Создайте суперпользователя
sudo -u pyland /opt/pyland/.venv/bin/python /opt/pyland/backend/src/manage.py createsuperuser

# Получите SSL (ОБЯЗАТЕЛЬНО!)
sudo certbot --nginx -d pylandschool.com -d www.pylandschool.com

# Готово!
```

---

## 🔄 Деплой обновлений

```bash
# Локально: подготовка
./nginx/scripts/prepare_deploy.sh

# На сервере: применить обновления
ssh root@78.40.219.145
cd /opt/pyland/backend/nginx/scripts
sudo ./deploy.sh
```

---

## 📚 Полная документация

- [INSTALL_GUIDE.md](INSTALL_GUIDE.md) - Пошаговая установка
- [DEPLOY.md](DEPLOY.md) - Краткая инструкция
- [nginx/README.md](nginx/README.md) - Подробная документация
- [nginx/DNS_SETUP.md](nginx/DNS_SETUP.md) - Настройка DNS

---

## ⚠️ Важно

- **Всегда** делайте commit + push **перед** установкой на сервер
- Скрипт `install.sh` клонирует код из GitHub (ветка main по умолчанию)
- Для dev/staging используйте: `git clone -b dev` и `GIT_BRANCH=dev sudo ./install.sh`
- HTTPS обязателен, сайт работает только через SSL
