name: Dependency Updates

on:
  schedule:
    # Каждый понедельник в 9:00 UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: 1.8.5
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Update dependencies
        run: |
          poetry update
          poetry lock

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet poetry.lock; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: 'chore: Update dependencies'
          title: '🔄 Weekly dependency updates'
          body: |
            ## Dependency Updates

            Автоматическое обновление зависимостей Poetry.

            ### Изменения
            - Обновлены версии пакетов в `poetry.lock`

            ### Проверка
            - [ ] CI тесты проходят
            - [ ] Нет критичных уязвимостей (проверить security job)
            - [ ] Совместимость с Django 5.2

            Создано автоматически workflow `dependency-updates.yml`
          branch: dependency-updates
          delete-branch: true
          labels: |
            dependencies
            automated

  security-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: 1.8.5

      - name: Install dependencies
        run: poetry install

      - name: Run safety check
        run: |
          poetry run safety check --json > safety-report.json || true

      - name: Upload safety report
        uses: actions/upload-artifact@v4
        with:
          name: safety-report
          path: safety-report.json
          retention-days: 30

      - name: Run bandit security scan
        run: |
          poetry run bandit -r src/ -f json -o bandit-report.json || true

      - name: Upload bandit report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report
          path: bandit-report.json
          retention-days: 30
