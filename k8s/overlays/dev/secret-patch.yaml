apiVersion: v1
kind: Secret
metadata:
  name: django-secret
  namespace: pyland
type: Opaque
stringData:
  # Development secrets (weak passwords OK for dev)
  POSTGRES_PASSWORD: "dev_password_123"
  SECRET_KEY: "django-insecure-dev-key-change-in-production"
  DATABASE_URL: "postgresql://pyland_user:dev_password_123@postgres-service:5432/pyland_db"
