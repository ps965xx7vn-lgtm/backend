apiVersion: v1
kind: Secret
metadata:
  name: django-secret
  namespace: pyland
type: Opaque
stringData:
  # Staging secrets - REPLACE WITH REAL VALUES
  POSTGRES_PASSWORD: "CHANGE_ME_staging_db_password"
  SECRET_KEY: "CHANGE_ME_generate_staging_secret_key"
  DATABASE_URL: "postgresql://pyland_user:CHANGE_ME@postgres-service:5432/pyland_db"
