apiVersion: v1
kind: Secret
metadata:
  name: django-secret
  namespace: pyland
type: Opaque
stringData:
  # Production secrets - MUST BE REPLACED WITH REAL VALUES!
  # Generate strong passwords: openssl rand -base64 32
  # Generate Django key: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

  POSTGRES_PASSWORD: "CHANGE_ME_STRONG_PRODUCTION_PASSWORD"
  SECRET_KEY: "CHANGE_ME_STRONG_DJANGO_SECRET_KEY"
  DATABASE_URL: "postgresql://pyland_user:CHANGE_ME@postgres-service:5432/pyland_db"
