apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: pyland
  labels:
    app: web
    component: django
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
        component: django
    spec:
      containers:
      - name: web
        image: ghcr.io/ps965xx7vn-lgtm/backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP

        # Health checks
        livenessProbe:
          httpGet:
            path: /api/health/
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 15
          failureThreshold: 5

        readinessProbe:
          httpGet:
            path: /api/health/
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 15
          timeoutSeconds: 12
          failureThreshold: 5

        # Environment variables from ConfigMap
        envFrom:
        - configMapRef:
            name: django-config
        - secretRef:
            name: django-secret

        # Resources
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "384Mi"
            cpu: "300m"

        # Command override for K8s
        command: ["gunicorn"]
        args:
          - "pyland.wsgi:application"
          - "--bind=0.0.0.0:8000"
          - "--workers=2"
          - "--timeout=60"
          - "--access-logfile=-"
          - "--error-logfile=-"
